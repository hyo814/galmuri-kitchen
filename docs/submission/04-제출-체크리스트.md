# 제출 체크리스트

원티드 AI Championship 2026 "AI 도구로 만든 나만의 서비스" 참가 접수(2026-09-18)와 과제 제출(2026-09-20)까지 할 일입니다.
제출 양식 항목은 [05-제출-양식-답안](05-제출-양식-답안.md)에 맞춰 두었고, 마감 전까지 수정할 수 있습니다.

- **지금 상태(9/19 밤)**: 참가 접수·1차 제출 끝. 5단계(요리 일기·집밥 리포트)까지 모든 기능이 배포돼 있고, 운영 주소 https://galmuri-kitchen.onrender.com 은 체험하기·`/?demo=1`로 로그인 없이 쓸 수 있습니다. 9/19에는 운영 주소에 대고 E2E 전체·3분 데모 동선·실제 사진 인식(냉장고 3장·손글씨 메모·영수증·레시피 사진)을 돌려 봤고(로컬 E2E는 예시 모드를 전제해 운영에서 다르게 나오는 항목이 있습니다 — 진짜 결함은 음식 찾기 중복 하나, 0절 13), 공통 보안 헤더와 제출 폼 답안까지 올렸습니다(0절 15~17). 밤에는 기본 주방 도구·소비기한 규칙 둘·사진 인식 칸 고르기를 더하고 캐시 성능 기준을 손봤습니다(0절 19) — **이제 SQLite·PostgreSQL 1,683개가 모두 통과합니다.** 남은 일은 바로 아래 **남은 일정**에 모았습니다.
- **바뀐 계획**: 9/18~19 리팩토링(`styles.css`·`FoodLogSheet.tsx` 나누기)은 취소했습니다(9/17 사용자 결정, Claude 사용량). **심사·투표 기간이 9/21부터 시작**(주최 측 안내)이라 리팩토링은 투표가 끝난 뒤에 합니다 — 기간 중에는 배포하지 않습니다(9/18 사용자 결정). 금~일은 새 기능 없이 실제 문제만 고칩니다.
- **테스트 담당**: 병합마다 Claude가 백엔드(SQLite·PostgreSQL)·`npm run check`·`npm run build`·E2E 전체를 돌리고, push 전후로 폰 크기 화면을 찍어 보여 줍니다. 사용자는 화면을 보고 push를 승인합니다(9/17 결정). 실기기에서만 되는 확인(홈 화면 추가, 오프라인, 삼성 인터넷, LTE)은 4절·8절에 선택으로 남겨 둡니다.
- 담당: **사용자**는 계정·결제·실기기·제출 권한이 필요한 일, **Claude**는 저장소 안에서 할 수 있는 일입니다.
- 수치 기준(9/19 23:47, `main` `ffd04fb`, 테스트도 같은 커밋에서 실행): 백엔드 테스트 1,683개(SQLite·PostgreSQL 모두 전부 통과, [6절 최종 테스트](#6-제출-자료) 참고), 프론트 검사 스크립트 11개, E2E(Playwright) 81개, 커밋 865개·병합 302개. 제출 문서(README·01·03·05)의 수치도 이 기준으로 맞춰 두었습니다.

## 남은 일정

- **9/20 마감 전 제출(사용자)**: 폼에서 두 칸을 고쳐 제출합니다 — ① **서비스 링크**를 `https://galmuri-kitchen.onrender.com/?demo=1`로(바로 체험, 지금 폼에는 로그인 화면이 먼저 뜨는 주소가 들어 있습니다), ② **AI 활용 칸**을 [05의 500자 문안](05-제출-양식-답안.md)으로. 제출 뒤에는 고칠 수 없습니다. 기술 스택 칩은 `Claude`·`React` 둘만 고릅니다.
- **최종 제출 직전(사용자)**: Render 환경변수 이름을 로컬 `.env`와 한 번 더 비교(Claude가 이름만 비교, 값은 읽지 않음). R2 4개·`FOOD_NUTRITION_API_KEY`는 9/17 확인 완료([2절](#2-키와-외부-계정))
- **마지막 배포 뒤(사용자)**: Auto-Deploy Off([8절](#8-투표-대비-917-밤-설정-921)), Health Check Path·체험 계정 지우기 Cron 기록 확인([3절](#3-배포))
- **9/19~9/20**: 새 기능 없이 운영 지켜보기, (권장) 9/20 23:59 전 폰 시크릿 탭 `/?demo=1` 한 번 더(9/18 확인 완료, [6절](#6-제출-자료)), (선택) 오프라인 마트 종이 영수증 사진. LTE 체험 IP 한도는 9/19 밤 확인 완료([3절](#3-배포)). **영수증 사진·Render 환경변수·Auto-Deploy 세 가지는 사용자가 마지막에 직접 하고 Claude에게 알려 줍니다(9/18 결정) — Claude는 먼저 묻지 않습니다.** 9/19 밤 기준: Render 환경변수는 넣었고, 마트 영수증과 Auto-Deploy Off는 9/20에 합니다(사용자).
- **9/21~ 심사·투표 기간**: 배포하지 않습니다(진행 중인 AI 요청이 끊기지 않게). `styles.css`·`FoodLogSheet.tsx` 나누기는 **투표가 끝난 뒤**로 — 종료일은 주최 측 공지를 확인해 여기에 적습니다. 미뤄 둔 작은 버그는 9/17 밤에 고쳤습니다.

## 0. 배포 기록

push하면 Render가 1분쯤 뒤 자동으로 배포합니다(Auto-Deploy를 끄기 전까지). push마다 Claude가 운영 스모크(`/` 200, `/api/me` 401, `/api/auth-options` 200)와 체험 계정 화면을 확인합니다.

| 완료 | 순서 | 할 일 | 담당 | 메모 |
|---|---|---|---|---|
| [x] | 1 | 저녁 폰 확인: 배포 주소에서 식단 탭(만들기 → 칸 채우기 → AI 초안 → 장보기 목록 만들기), 로그인 로고·지난번 로그인, 카메라/앨범 버튼 | 사용자 | 9/16(수) 저녁. 완료 — 9/17 테스트는 Claude 담당으로 사용자가 결정, Claude의 폰 크기(384×832) 화면 확인 + 사용자 스크린샷 승인으로 대신함. |
| [x] | 2 | 새 기능 확인: 재고 사진 여러 장·구입일 칩·`기억 안 나요`, 레시피 사진 5장, 체험 계정 예시 식단, 내보내기 `meals.csv`·`food_logs.csv`·`cook_logs.csv` | 사용자 | 완료 — Claude 폰 크기 화면 확인 + 사용자 스크린샷 승인(9/17). |
| [x] | 3 | `git push origin main` | 사용자 | 완료(`b560ed4`). 심사 기간에는 큰 기능을 폰 확인 없이 올리지 않습니다. 마지막 배포 뒤 자동 배포를 끄는 일은 8절에 있습니다. |
| [x] | 4 | 배포 끝난 뒤 스모크: `/` 200, `/api/me` 401, `/manifest.webmanifest` 200, 체험하기 → 식단 탭에 예시 식단 | 사용자 + Claude | 9/15 낮 확인값: 세 주소 모두 기대대로(배포본 `4d86b25`). 9/17 재확인: `/` 200, `/api/me` 401, `/api/auth-options` 200, 체험하기 → 식단 탭 흐름 정상. |
| [x] | 5 | Render 웹 서비스 → **Shell**: `flask --app app seed-default-channels`(9/15 사용자 완료) | 사용자 | 기본 요리 채널 14개. 로그인 계정의 영상 칸에 쓰입니다(체험 계정은 받아 둔 기본 채널 영상을 읽기만 함, 없으면 예시). |
| [x] | 6 | `git push origin main`(`ad2b398`) | 사용자 | 완료. 체험 식단 고치기 — 빈 날 채우기(저녁 매일·점심 사흘·아침 하루), 양념 묶음 규칙, 장보기 수량 소수점, 나트륨 이상 표시, 먹었어요 줄바꿈. |
| [x] | 7 | `git push origin main`(`5ce8c8a`) | 사용자 | 완료. 요리 일기 직접 쓰기(내 레시피 고르기·레시피 없이 쓰기), 추천 레시피 요리했어요, 재료비 칸 `재료비 (선택)`·도움말 `N인분을 합친 값이에요`. |
| [x] | 8 | `git push origin main`(`1f89260`) | 사용자 | 완료. 여러 요리 가져오기 — 링크 한 페이지에 요리가 여러 개면 목록으로 모두 찾아 골라서 가져오기(최대 5개). |
| [x] | 9 | `git push origin main`(`901fa47`) | 사용자 | 완료. 레시피 상세 재료 줄에 재고 남은 양(`있어요 300g`)·요리 직후 `다 썼어요`, 장보기 쇼핑몰에서 한꺼번에 찾기, 쿠팡·네이버 쇼핑·이마트몰 낮은 가격순. |
| [x] | 10 | `git push origin main`(`9410577`) | 사용자 | 완료. 쇼핑몰 6곳 모두 낮은 가격순(컬리·롯데마트 제타·G마켓 추가, 홈플러스 뺌), 식단 스크린샷 다시 찍기·39번 추가, 제출 문서·체크리스트 갱신. |
| [x] | 11 | `git push origin main`(미뤄 둔 버그 수정, 9/17 밤) | 사용자 승인 | 화면 읽기·포커스 7건, 화면 동작 9건(메모 닫기 전 확인, `50원 미만`, 새 탭 열기 등), 서버·로그인 3건(로그인 뒤 보던 화면 유지, 출처 주소 검사, 같은 링크 다시 가져오기 안내). 검토 3건 통과, 백엔드 1,608개(SQLite·PostgreSQL), `npm run check`·`build`, E2E 79개 통과 뒤 push. |
| [x] | 12 | `git push origin main`(`bbae138`) | 사용자 | 완료. 기본 필수품 72개를 모든 사용자에게(가입 시드 + 기존 사용자 한 번 채우기), 배너는 한 번이라도 있었던 필수품만, 필수품↔재료 이름 비교 규칙 분리. 로그인 말풍선 다크 테마 고침도 같은 날 push(`386b8f9`). |
| [x] | 13 | `git push origin main`(`4d2a0c3`, 9/19 00:38) | Claude | 운영 화면을 폰 크기로 훑다 나온 읽기 어려운 곳 셋. ① **음식 찾기에 같은 이름이 줄줄이 서던 것** — 운영에서 `비빔밥`이 이름 똑같은 줄 6개로 나왔습니다(캐시에 `콩기름` 18개·`엑스트라버진올리브유` 15개처럼 흔함). 정렬 뒤 20개로 자르기 전에 같은 이름을 하나로 줄입니다(`foods.unique_by_name`, 설계 문서 결정 10 개정 3). ② 먹은 기록 날짜 시트 `kcal을 계산할 수 있는 기록이 없어요` → `남긴 기록에 kcal 정보가 없어요`(기록이 보이는 화면과 어긋나 읽힘). ③ 사진으로 추가 `예시 사진도 1번으로 세요` → `세어요`(`-세요`가 명령형으로 읽혀 주어가 사용자로 뒤집힘). README 수치 2건(체험 필수품 73개, 설계 31절)도 함께. 배포 뒤 운영 API로 검색어 5개 × 엔드포인트 2개 모두 중복 0종·목록 20줄 유지를 확인했습니다. |
| [x] | 14 | `git push origin main`(`cf4b9f5`·`cfb3794`, 9/19 00:54·01:01) | Claude | 운영 화면 12장을 관점 7가지로 다시 보고(첫인상·공개 투표·UX 흐름·카피·시각·접근성·AI 전달), 제안마다 반증 검증을 거쳐 살아남은 것만 반영했습니다(18건 중 15건 적용, 반증 폐기 26건). **제출 자료 사실 오류**: 슬라이드 3번이 `쇼핑몰 7곳`인데 실제는 6곳이고 품은 스크린샷에 9/17에 뺀 홈플러스가 찍혀 있어, 문구를 고치고 운영에서 같은 시트를 1152×2496으로 다시 찍어 슬라이드를 다시 구웠습니다(6절). **첫인상**: 폰에서는 `index.html`의 소개 한 줄이 `display:none`이라 이 앱이 뭔지 알 방법이 없었습니다 → 체험 안내 카드 첫 문장에 `갈무리부엌은 재료가 상하기 전에 알려주고, 그 재료로 만들 요리를 찾아줘요`. **접근성**: 별점 `--star` 2.27:1 → 4.6:1(WCAG 1.4.11 미달 해소), 첫 화면 `예시 사진 읽기` 버튼 경계 1.12:1 → 3.61:1. **빈 화면**: 재고·주방 도구가 다른 탭과 달리 다람이·얻는 것 한 줄이 없었습니다. **문구**: `사진 찍어보기` → `예시 사진 읽기`(심사위원은 냉장고 앞에 없습니다), 달력 범례·`항목` 개발 말·붙임 보조용언 4곳. |
| [x] | 15 | `git push origin main`(`903bd81`·`66312ff`, 9/19 08:26·08:29) | Claude | **공통 보안 헤더** — 사진·내보내기 같은 일부 응답에만 붙던 보안 헤더를 `after_request`로 모든 응답에 붙입니다(`nosniff`, `X-Frame-Options: DENY`, CSP `frame-ancestors 'none'`, `Referrer-Policy`, `Permissions-Policy`, 운영에서만 HSTS). 이미 더 센 값을 정한 응답은 `setdefault`라 그대로입니다. CSP 소스 제한은 R2 사진·유튜브 섬네일·공공 레시피 사진이 깨질 수 있어 투표 뒤로 미뤘습니다(설계 9절). 테스트 3개 추가(`test_security_headers.py`). 약관 5절에 **레시피 가져오기** 조항(내 계정 안에서만 보관, 재배포 안 함, 출처 사진은 저장하지 않고 주소만 기록, 권리는 원저작자, 요청하면 지움)도 함께 적었습니다. 이어서 크롬이 지원을 뺀 `interest-cohort`를 `Permissions-Policy`에서 빼 콘솔 경고를 없앴습니다. |
| [x] | 16 | `git push origin main`(`ca3516b`·`164384b`·`17a96b4`, 9/19 09:02~09:09) | Claude | 운영 화면에서 남은 시각·문구 셋. ① 먹은 기록 달력의 `요`(요리 일기) 뱃지가 14px·9px이라 얼룩으로 보이던 것 → 16px·11px, 배경 `--warn`·글자 `--warn-tint`(카드 대비 1.10:1 → 읽히는 대비). 뱃지는 `position: absolute`라 칸 배치는 그대로입니다. ② 요리 일기 목록에서 사진 없는 자리가 카드에 뚫린 구멍처럼 보이던 것(`--field` → `--neutral-tint`). ③ 필수품 빈 화면 `아직 등록한 필수품이 없어요.` → `아직 필수품이 없어요.`(`등록`은 개발 말). 슬라이드 2번의 옛 화면도 다시 찍어 바꿨습니다. |
| [x] | 17 | `git push origin main`(`623ffd4`, 9/19 09:34~10:56) | Claude | 4절 남은 확인과 제출 자료. **실제 사진으로 확인**: 사용자가 준 진짜 냉장고 사진 3장(양념 바구니·선반·문칸)을 한 번에 읽어 재료 16개를 보관 위치까지 나눠 찾고 AI 한도는 1번만 썼습니다. 진짜 손글씨 메모에서는 살 것 10개를 읽고 수세미·행주를 생활용품으로 갈라냈습니다. 링크·사진 레시피 가져오기도 운영에서 확인했습니다. **1분 데모 영상**: 운영 주소에 대고 3분 동선을 한 번에 돌려 자막을 얹어 녹화했고(1분 10초, 소리 없음) README와 [`docs/submission/demo/`](demo/)에 넣었습니다 — 제출 폼에는 영상 칸이 없습니다. **제출 폼 실물 확인**: AI 활용 칸이 500자 제한이고 폼에 든 수치가 초반 값(29시간·커밋 289개·테스트 802개)이라 479자 문안을 새로 써 05에 넣었고, 서비스 링크도 `/?demo=1`로 바꿔야 한다고 적었습니다. 상표 조사(KIPRIS)는 절차만 체크리스트에 적어 두었습니다. |
| [x] | 18 | `git push origin main`(`00b9d16`~`f1a5fff`, 9/19 11:24~20:13) | Claude | 문서 수치를 `623ffd4` 기준으로 맞추고 사실 오류 넷을 고쳤습니다(`00b9d16`). **README 데모 영상 바로 재생**. GitHub는 저장소 파일을 `<video>`로 틀어 주지 않아, 영상을 이슈 첨부(user-attachments)로 올린 주소를 씁니다. 다듬은 것: 가운데 정렬로 오른쪽 여백 줄이기 → 첫 화면 표지 깔기(흰 화면으로 뜨던 것) → GitHub가 지우는 표지 속성 빼기 → 제목이 `galmuri-kitchen-1min-demo.webm`으로 뜨게 영문 파일 이름으로 다시 올리기 → 접기 → 썸네일 → 초록 버튼 한 줄 → 다시 바로 재생, 앞 2초 흰 화면 잘라내기 → 화질이 떨어져 원본과 같은 VP8 첨부본으로 되돌리기. 내려받을 파일은 [`demo/갈무리부엌-1분-데모.webm`](demo/)에 그대로 둡니다. **Render 환경변수**는 사용자가 넣어 2절 항목을 완료로 표시했습니다. **코드 변경 없음** — `git diff 623ffd4..f1a5fff -- backend frontend render.yaml`가 비어 있어 테스트 수치는 17번과 같습니다. |
| [x] | 19 | `git push origin main`(`9222f8a`~`ffd04fb`, 9/19 23:26~23:47) | Claude | **기본 주방 도구 13개**(프라이팬만 6개월 점검 — 설계 18절 프리셋. 나머지는 주기를 비워 뒀습니다, 없는 도구의 `점검할 때`를 막으려고. 프라이팬도 구입일·마지막 점검일이 비어 기준일이 만든 날이라 넣은 직후에는 안 뜹니다). **가공유 24일·유산균음료 26일** 소비기한 규칙을 실제로 적는 이름(딸기·초코·바나나우유, 야쿠르트)에 붙였습니다 — 식약처 1차 공개분 10개 유형 중 남아 있던 둘입니다. 프레스햄 66일은 넣지 않았습니다(이름에 `햄`이 들어가면 규칙 매칭이 짧은 쪽인 햄 57일을 고릅니다). **사진 인식이 보관 위치 칸까지 고릅니다** — 사용자가 만든 칸 이름을 프롬프트에 넘겨(`ai.locations_hint`) AI가 `3층`·`야채칸`처럼 뜻이 담긴 이름을 고르게 했습니다. 목록에 없는 이름은 버리고 예전처럼 종류로 고릅니다(모델이 지어낸 이름·사진 속 글이 칸 선택을 흔들지 못하게). 재고에 넣지 않는 memo는 칸 이름을 넘기지 않습니다. **캐시 성능 기준**을 첫 호출 대비로 바꿔 PostgreSQL 숙제를 풀었습니다(위 6절). 기존 사용자 백필 마이그레이션 둘(`h6t6o6o6l6s6` 도구, `h7r7u7l7e7s7` 규칙)은 배포 때 `flask db upgrade`로 적용됩니다(`Dockerfile`). 같은 push에 **다른 Claude 세션**의 작업(장보기 밀가루 추천 3개, 고기 양념장 비율 참고 자료)도 함께 올라갔습니다. |

## 1. 요강과 접수

| 완료 | 할 일 | 담당 | 기한 | 메모 |
|---|---|---|---|---|
| [ ] | 요강 원문 확인 | 사용자 | 제출 전 | 양식 항목(대표 이미지·제목·문제·AI 활용·스택·링크·스크린샷 5장)은 05에 반영했습니다. 분량 제한·영상 여부·저장소 공개 여부가 요강에 있으면 Claude에 붙여 넣습니다. 9/16 주최 측 안내(설치 파일 금지, 로그인 없이 사용 가능, GitHub 링크는 서비스 링크 아님)를 확인해 `/?demo=1`로 맞췄습니다. |
| [x] | 참가 접수 | 사용자 | **9/16 오전** | 팀/개인 구분, 서비스 이름 `갈무리부엌`. 9/16 완료 |
| [x] | 저장소 공개 여부 결정 | 사용자 | 9/16 | 결정: 공개 가능. Claude가 9/17 전체 git 기록(모든 커밋)을 훑어 비밀값 없음을 확인했습니다. |
| [x] | 작성자·연락처 칸 채우기 | 사용자 | 9/16 완료 | README와 `docs/site/portfolio.html`의 `[이름]`·`[이메일]`·`[GitHub]`, `product.html` 바닥글의 `[회사명]`·`[제휴·투자 문의]`(문의 이메일은 채움, 8절 7번) |

## 2. 키와 외부 계정

운영 값은 `.env`가 아니라 Render 환경 변수에 넣습니다. 이름 목록은 `backend/.env.example`과 `render.yaml`에 있습니다.

| 완료 | 할 일 | 담당 | 기한 | 메모 |
|---|---|---|---|---|
| [x] | Claude API 월 사용 한도와 잔액 확인 | 사용자 | 9/16 | 크레딧이 떨어지면 AI 기능이 오류로 끝납니다. `DEMO_AI_GLOBAL_DAILY=80`은 월 한도 $30에 맞춘 값입니다(2026-09-15 한도 상향: 로그인 20/20·체험 5번). 월 한도 $30은 9/15 올림(완료). 잔액 $24(9/15 확인). 심사 기간 중 한 번 더 잔액을 봐요(잔액이 0이면 한도와 관계없이 AI가 오류로 끝나요). |
| [x] | R2 네 값이 Render 웹 서비스·Cron 둘 다에 있는지 | 사용자 | 9/16 | 완료 — 9/17 사용자 확인. 없으면 메모 사진이 `사진을 지금은 올릴 수 없어요.`로 막힙니다. |
| [x] | 공공 레시피 전체 동기화 여부 확인 | 사용자 | 9/16 | 완료 — 사용자가 12건보다 많은 공공 레시피를 확인했습니다(전체 동기화됨). 추천 칸의 식약처 레시피가 12개뿐이면 예시 레시피 상태입니다 → Render Shell `flask --app app sync-public-recipes`(`FOODSAFETY_API_KEY` 필요) |
| [x] | 기본 요리 채널 목록 확정 | 사용자 + Claude | 9/14 | 14개(`15df7e0`). 넣기는 0절 5번 |
| [x] | 카카오·네이버 로그인 Redirect URI 등록 | 사용자 | 9/15 | 배포 주소 기준 |
| [x] | 구글 로그인 Redirect URI 등록 | 사용자 | 9/16 | 완료 — 9/17 확인: 운영 `/auth/login/google` → 구글 로그인 화면이 redirect_uri 오류 없이 뜸. OAuth 동의 화면 게시 상태(프로덕션) 확인은 사용자 몫. |
| [x] | 구글 OAuth 동의 화면 게시 상태가 `프로덕션`인지 | 사용자 | **9/17** | 9/18 사용자가 운영에서 구글 로그인·로그아웃까지 확인했습니다. 다른 사람 계정으로도 되려면 게시 상태가 `프로덕션`이어야 합니다. Google Cloud Console → OAuth 동의 화면(대상). `테스트`면 테스트 사용자만 로그인됩니다. 요청 범위가 `openid profile`뿐이라 `앱 게시`해도 심사가 필요 없습니다. |
| [ ] | 최종 제출 직전 Render 환경변수 이름을 로컬 `.env`와 비교 | 사용자 + Claude | 제출 직전 | Claude가 이름만 비교합니다(값은 읽지 않음). **사용자가 마지막에 직접 처리합니다(9/18 결정) — Claude는 이 항목을 따로 묻지 않습니다.** |
| 보류 | (선택) 쿠팡 파트너스 `COUPANG_ACCESS_KEY`·`COUPANG_SECRET_KEY` | 사용자 | 투표 뒤 | **9/15 판단: 심사·투표 기간 안에는 키를 받을 수 없어 이번 제출에서는 넣지 않습니다.** 누적 판매 15만 원을 넘겨 최종 승인이 나면 그때 넣습니다. 코드는 준비돼 있어 키만 넣으면 작동합니다. 없으면 일반 검색 링크입니다. 넣으면 파트너스 활동 채널에 배포 주소를 등록하고, 폰에서 쿠팡 칩이 `link.coupang.com`으로 가는지 봅니다. **키 발급 조건(9/15 확인)**: 쿠팡 파트너스 이용 가이드(2024.12) `STEP 3. 최종승인`에 "최종 승인이 완료되어야 … 파트너스 API 이용이 가능"하고 "누적 판매금액이 15만 원 이상 시 자동으로 최종승인 채널 검토 진행"이라고 적혀 있습니다. 실적이 없는 새 계정은 심사·투표 기간 안에 키를 받기 어렵습니다. 출처: https://partners.coupang.com/api/v1/partners-guide (PDF) |
| [x] | `FOOD_NUTRITION_API_KEY`가 Render에 있는지 | 사용자 | **9/16** | 완료 — 9/17 운영 `/api/me`의 `nutrition`이 `on`(키가 있을 때만 on). 영양 계산(식품영양성분 DB, 공공데이터포털 키). 4b-2가 push돼 있어, 키가 없으면 운영에서 영양 칸이 숨겨집니다(`foods.nutrition_mode` off). 기존 식약처 레시피 키로는 호출되지 않는 것을 조사에서 확인했습니다. |

## 3. 배포

절차는 [`docs/deploy.md`](../deploy.md)에 있습니다.

| 완료 | 할 일 | 담당 | 기한 | 메모 |
|---|---|---|---|---|
| [x] | Render Blueprint로 웹·PostgreSQL·체험 계정 지우기 Cron 만들기 | 사용자 | 9/14 | 유료 인스턴스(웹 0.5c-512mb, DB 0.1c-256mb, Singapore)라 잠들지 않습니다. |
| [x] | 배포 주소를 README·제출 문서에 넣기 | Claude | 9/14 | https://galmuri-kitchen.onrender.com |
| [x] | 로컬 검증: 백엔드 테스트 SQLite·PostgreSQL | Claude | 9/15 | `f11ed6d`에서 1,067개 둘 다 통과. `b560ed4`에서는 SQLite 1,475개 통과(PostgreSQL은 6절 최종 테스트에서 다시) |
| [ ] | Render Health Check Path | Claude → 사용자 | 9/17 밤 | `render.yaml`에 추가 예정(fix/voting-readiness)이었고 `b560ed4`에서 병합됐습니다(`/api/auth-options`). 배포 뒤 대시보드에 반영됐는지 사용자가 봅니다. `/api/me`는 로그인 전 401이라 쓰지 않습니다. |
| [x] | 체험 계정 지우기 Cron Job 실행 기록 확인 | 사용자 | 9/19 밤 | `flask --app app purge-demo-users`, 매시. **9/19 밤 사용자가 Render Shell에서 직접 돌려 `체험 계정 0개를 지웠어요.`를 받았습니다** — 명령이 운영에서 도는 것을 확인했고, 지울 만큼 오래된 체험 계정이 그때 없었다는 뜻입니다. |
| [x] | (선택) 체험 계정 IP 한도 확인(`docs/deploy.md` 5-3) | 사용자 + Claude | 9/19 밤 | 실제 폰 LTE에서만 확인할 수 있습니다. 8절 1번과 같은 일이고, **9/19 밤 확인했습니다** — 자세한 내용은 [8절 1번](#8-투표-대비-917-밤-설정-921). |

## 4. 폰 점검 (갤럭시 S22 Ultra)

9/17부터 기능 확인은 Claude가 폰 크기 화면으로 하고 사용자가 스크린샷으로 승인합니다. 아래는 실기기에서만 확인되는 것 위주로, 시간이 날 때 하는 **선택** 항목입니다.

| 완료 | 할 일 | 담당 | 기한 | 메모 |
|---|---|---|---|---|
| [x] | 크롬에서 **홈 화면에 추가** → 아이콘으로 열기 | 사용자 | 9/16 | **9/19 사용자가 직접 확인했습니다.** |
| [x] | 체험하기 → 재고 배지·필수품 배너 | 사용자 → Claude | 9/16 | **9/19 확인 완료** — 운영 배포에서 Claude가 폰 크기(384×854) 브라우저로 확인. 배지(두부 D-1·대파 D-2·김치 `구입 10일째`)와 필수품 배너(간장) 모두 정상. 운영 E2E `fridge.spec` 배지·배너 테스트 2건 통과. |
| [x] | 영수증·냉장고 사진 2~3장 한 번에 → 구입일 칩 → 재고에 넣기 | 사용자 → Claude | 9/16 | **9/19 확인 완료** — 운영 체험 계정에 예시 영수증(`/samples/ereceipt.jpg`) 2장을 한 번에 올려 **실제 Claude로** 읽었습니다. 결과: `찾은 재료 6개`, 안내 `사진 2장에서 찾았어요 · 겹친 재료는 합쳤어요`, 품목마다 수량·보관 위치·가격(애호박 1개 실온 1,580원, 노르웨이 고등어 1팩 냉동실 24,980원 등), **영수증에 찍힌 날짜를 읽어** 구입일 칩이 `8월 18일`로 골라지고 `✓ 영수증 날짜로 채웠어요` 표시. **9/19 추가 — 사용자가 찍은 진짜 냉장고 사진 3장(싱크대 양념 바구니·냉장고 안 선반·냉장고 문칸)으로 다시 확인했습니다.** 49.9초에 `찾은 재료 16개`, `사진 3장에서 찾았어요 · 겹친 재료는 합쳤어요`, **스캔 한도 0→1로 한 번만** 줄었습니다. 싱크대 바구니는 `실온`, 냉장고 안·문칸은 `냉장실`로 보관 위치를 나눠 잡았고(식초 2병·올리브오일·참기름·물엿·조청·간장 2병·올리고당 / 고추장·고춧가루·깨소금·라유·두부·계란 13개·마요네즈·마늘소스), 문칸 두 칸에 걸친 **계란을 13개로 세고**, 사진 사이에 겹친 식초·간장을 합쳤으며, 일본어 라벨 `ラー油`도 `라유`로 읽었습니다. 영수증이 없어 구입일을 모르는 경우라 `냉장고에 있던 재료는 대략 골라도 괜찮아요` 안내가 함께 나옵니다. 앞서 `여러 장이 한도 1회`는 코드로도 확인했습니다 — `backend/app/scan.py:225-226`이 사진 수와 무관하게 `start_ai_call`을 한 번만 불러 `AiCall` 한 줄만 남기고 `ai.extract(kind, images)`도 한 번만 호출합니다(주석에 그 의도가 적혀 있습니다). |
| [x] | AI 레시피 3개, 링크 가져오기(블로그 주소 1개), 레시피 사진으로 가져오기 | 사용자 | 9/16 | 체험 계정은 AI 레시피 한도 하루 5번을 AI 레시피·가져오기·AI 식단 초안이 함께 씁니다. 시연용 블로그 주소를 정해 둡니다. **9/19: AI 레시피 3개는 운영에서 실제 Claude로 확인 완료** — 29.6초, `두부·대파를 먼저 넣어 3개 만들었어요`처럼 임박 재료를 먼저 쓰고, 카드마다 인분·조리 시간·`재료 7개 중 4개 있어요`·없는 재료 칩·`자세히`/`저장`이 나옵니다. 한도는 `/api/ai-usage`로 재서 0→1(한도 5)로 **한 번만** 줄었습니다. **링크 가져오기도 9/19 확인 완료** — 시연용 주소는 `https://m.eggiscoming.com/article/event/8/4904/`(우주떡집 고흥 메뉴 레시피)로 정했습니다. 이 페이지는 레시피가 **글 없이 이미지로만** 있는데, 앱이 본문 사진을 읽어 `요리가 5개 있어요`(오리지날떡볶이·부트졸로키아 먹지파가·브로콜리노 바질리아노·옥황상제떡볶이·명란바지락즈타)로 뽑았습니다. 요리마다 재료 20~30개·만드는 법 5~7단계, 줄마다 `사진에서 읽었어요`·`양이 안 보이는 재료 N개` 표시, 아래에 `본문 사진이 많아 앞쪽 5장만 읽었어요`와 `AI는 1번만 썼어요`가 함께 나옵니다. 여러 요리 가져오기(17절)와 사진 읽기를 한 화면에서 같이 보여줄 수 있어 텍스트 블로그보다 시연에 낫습니다. 실측: AI 한도 0→1, 1분 40초. **사진으로 가져오기도 9/19 확인 완료** — 같은 페이지의 레시피 이미지 한 장을 올려 `부트졸로키아 먹지마라` / 재료 29개(가래떡 40cm 1, 대파 한 줌, 우삼겹 한 줌, 어묵 5조각…)를 읽고 확인 폼까지 채웠습니다. 글이 없는 사진에서도 재료·만드는 법을 뽑습니다. |
| [x] | 손메모 사진 → 살 것 뽑기, 살 것 추가의 `사진에서 뽑기`, 메모 사진 올리기 | 사용자 → Claude | 9/16 | **9/19 확인 완료 — 사용자가 준 진짜 손글씨 메모로 다시 확인했습니다.** 손으로 쓴 10개(대파·마늘·당근·오이·소고기·양배추·버섯·두부·수세미·행주)를 **10/10 전부** 읽었고(14.1초), **수세미·행주를 `생활용품`으로 구분**했으며, 이미 장보기 목록에 있는 대파·두부·수세미는 `목록에 있어요`로 표시하고 체크를 꺼서 담기 버튼이 `7개 장보기에 담기`(10−3)로 나왔습니다. 시연 시나리오가 말하는 동작 그대로입니다. 앞서 예시 모바일 영수증으로도 같은 길을 확인했습니다(살 것 7개, 재사용봉투를 생활용품으로 구분). 15.5초에 `찾은 살 것 7개`(애호박·체리·고등어·재사용봉투·소갈비·돈육·새우)를 뽑고, **재사용봉투를 `생활용품`으로 구분해 표시**했습니다. 7개 모두 장보기에 담겼습니다. 메모 스캔은 AI 한도를 `recipe`가 아니라 `scan` 쪽으로 셉니다(실측). R2 연결은 먹은 기록 사진 테스트(`more.spec`)가 운영에서 통과해 함께 확인됐습니다. |
| [x] | 오프라인: 장보기 한 번 연 뒤 비행기 모드 → 앱 닫고 다시 열기 → 체크 → 비행기 모드 끄기 | 사용자 → Claude | 9/16 | **9/19 확인 완료** — 운영 배포에서 Claude가 폰 크기(384×854) 브라우저로 확인(서비스워커를 켠 채). 연결을 끊고 새로고침해도 앱과 장보기 목록이 열리고, 끊긴 채 체크한 것이 연결 뒤 서버(`/api/shopping`의 `done_at`)까지 저장되는 것을 확인했습니다. |
| [x] | 식단 탭: 체험 계정 예시 식단 → 장보기 목록 만들기 → AI 초안 → 넣기 | 사용자 | 9/16 | 인분·kcal 표시, 초안은 1분쯤 걸림. 폰 확인은 스크린샷으로 대신(9/17, 목표 정하기→주 보기→월 보기→장보기 목록 만들기 흐름: `21`·`22`·`24`). **9/19 전부 확인 완료** — AI 초안까지 운영에서 실제 Claude로 확인했습니다(1분 12초, `28칸 중 11칸` → `15칸`, AI 한도 0→1로 한 번만 줄었고 예시 결과 안내는 뜨지 않았습니다). 앞서 확인한 나머지(주/월 보기·칸 채우기·장보기 목록 만들기)는 운영 배포에서 Claude가 폰 크기(384×854) 브라우저로 확인 — 운영 E2E `meals.spec` 6건 통과. AI 초안만 남았습니다(체험 AI 예산 하루 80콜을 아끼려고 미룸).** |
| [x] | 더보기 → 데이터 내보내기에 식단 줄, 받은 zip에 `meals.csv` | 사용자 → Claude | 9/16 | **9/19 확인 완료** — 운영 배포에서 Claude가 폰 크기(384×854) 브라우저로 확인. 운영 E2E `more.spec`의 내보내기 테스트가 zip 수신까지 통과했습니다(같은 파일의 먹은 기록·내 몸 정보·탈퇴 테스트 포함 12/13 통과). |
| [x] | 다크 테마에서 재고·장보기·레시피 상세·식단 | 사용자 → Claude | 9/16 | **9/19 확인 완료** — 운영 배포에서 Claude가 폰 크기(384×854) 브라우저로 확인(`prefers-color-scheme: dark`). 재고·장보기·레시피 상세 세 화면을 찍어 확인했고, `<html>`에 `data-theme="light"`가 붙지 않는 것과 body 배경이 어두운 값인 것도 함께 확인했습니다. 식단은 9/17 `27-meals-dark`. |
| 보류 | 삼성 인터넷에서도 체험하기·오프라인 한 번 | 사용자 | 9/16 | **9/19 넘어가기로 결정** — 브라우저를 따로 설치해야 해서 하지 않습니다. 크롬에서 오프라인·홈 화면 추가를 확인했고, 서비스워커·manifest는 표준이라 삼성 인터넷(크로미엄 계열)에서도 같게 동작할 것으로 봅니다. |
| [x] | 점검에서 나온 문제 고치기 | Claude | 9/17 | 완료 — 9/17 여러 번 push(0절 6~11번). 미뤄 둔 작은 버그도 9/17 밤에 고쳤습니다. |

## 5. 쇼핑몰 주소 확인

| 완료 | 할 일 | 담당 | 기한 | 메모 |
|---|---|---|---|---|
| [x] | 쇼핑몰 6곳 × 정렬 링크를 폰에서 열어 결과 적기 | 사용자 | 9/16 | [확인표](../superpowers/store-links-phone-check.md). 검색어 유지, 정렬 적용, 앱으로 넘어갈 때 동작. 완료 — 9/17 사용자 폰 확인으로 쿠팡·네이버 쇼핑·컬리·이마트몰·롯데마트·G마켓 6곳 모두 `낮은 가격순` 통과(홈플러스는 목록에서 뺐습니다). |
| [x] | 통과한 칸만 `storeLinks.ts`에 반영(`verified` 날짜) | Claude | 9/17 | 완료 — 6곳 모두 `낮은 가격순` 하나만 반영(`fix/store-sorts`), 롯데마트는 롯데마트 제타 주소, 홈플러스는 목록에서 뺐습니다. |

## 6. 제출 자료

| 완료 | 할 일 | 담당 | 기한 | 메모 |
|---|---|---|---|---|
| [x] | 제품 페이지·케이스 스터디·제출 문서 01~05를 `f11ed6d` 기준으로 고치기 | Claude | 9/15 | `docs/site/`, `docs/submission/` |
| [x] | 제품 페이지·01·04·05·README를 `b560ed4` 기준으로 다시 고치기(영양·먹은 기록 반영, 수익 구조 추가) | Claude | 9/15 | `docs/voting-readiness`. 케이스 스터디·02·03은 이번에 손대지 않았습니다. |
| [x] | 새 스크린샷 찍기(아래 목록) | Claude | 9/16 | 운영 주소 체험 계정으로. 기존과 같은 1152×2496(PC 컷만 2560×1600) |
| [x] | 스크린샷 교체, README 표, `form/` 16:9 이미지 다시 만들기 | Claude | 9/16 | 바뀐 파일은 같은 이름으로 바꾸고, 새 파일은 README 재고·레시피·식단·다크·체험 방법 절에 표로 넣었습니다. |
| [ ] | (선택) 시연 사진 준비: 영수증 1장, 냉장고 사진 1장, 손메모 1장 | 사용자 | 9/20까지 | [시연 시나리오](02-시연-시나리오.md) 준비물 **사용자가 마지막에 직접 처리합니다(9/18 결정) — Claude는 이 항목을 따로 묻지 않습니다.** **9/19: 손메모는 사용자가 두 장 주셨고 운영에서 인식까지 확인했습니다**(`~/Downloads/시연-손메모-1-생활용품포함.jpg`, `시연-손메모-2-식재료만.jpg`). 1번은 생활용품이 섞여 있어 시연에 더 좋습니다. **냉장고 사진도 9/19 사용자가 세 장 주셨고**(`~/Downloads/시연-냉장고-1-양념바구니.jpg`·`2-냉장고안.jpg`·`3-문칸.jpg`) 운영에서 인식까지 확인했습니다. 모바일 영수증 캡처는 이미 앱에 예시로 들어 있습니다(`/samples/ereceipt.jpg`). **오프라인 종이 영수증 한 장만 남았습니다.** |

| [x] | (선택) 3분 데모 리허설 2번(시간 재기) | 사용자 → Claude | 9/20까지 | **9/19 Claude가 운영 주소에 대고 3분 동선을 한 번에 돌려 실측했습니다.** 조작만 **52초**(영수증 AI 읽기 10.9초 · AI 레시피 23.0초 · 나머지 14단계 각 1초 안팎). 3분 중 2분 이상을 말하는 데 쓸 수 있어 여유롭습니다. 문서 예상(각 30초)보다 AI가 빨라, `30초쯤 걸립니다`라고 말해 두면 먼저 끝납니다. 가장 긴 대기는 AI 레시피 23초 하나뿐이라 그때 할 말만 준비하면 빈 시간이 없습니다. |
| [x] | (선택) 1분 소개 영상 녹화·자막 | 사용자 → Claude | 9/20까지 | [콘티](02-시연-시나리오.md#1분-소개-영상-콘티). **9/19 Claude가 자막을 넣어 녹화했습니다** — 384×854, **1분 10초**, webm. 실제 Claude 호출 2회(영수증 스캔·AI 레시피)가 그대로 담겼고 음성은 없습니다(사용자 결정). 헤드리스 브라우저 캡처라 실기기 느낌은 아니니, 폰으로 다시 찍을 때 콘티로 쓸 수 있습니다. 파일은 사용자 `~/Downloads`에 전달했습니다. |
| [x] | 최종 테스트 다시 돌리기 | Claude | 9/19 밤 | `ffd04fb`에서 다시 돌렸습니다. **SQLite 1,683개·PostgreSQL 1,683개 모두 전부 통과**(`-W error::DeprecationWarning`, 47초 / 150초), `npm run check`(11개)·`npm run build` 통과, **E2E(Playwright) 81개 통과**(46초). 9/17부터 PostgreSQL 전체 실행에서만 넘던 성능 기준 1건(`test_recommendations_first_call_and_cached_call_with_large_inventory`)은 **기준을 절대 시각에서 첫 호출 대비로 바꿔 풀었습니다**(`bf99a4d`, 사용자 결정 9/19 밤). 재던 것이 `캐시가 실제로 듣는지`라 첫 호출 대비(`캐시 호출 < 첫 호출 / 5`)가 더 맞고, 앞서 도는 테스트 수·머신에 흔들리지 않습니다. 9/19 낮에는 이 기준이 SQLite 전체 실행에서도 넘나들기 시작했습니다(0.133초) — 기준선이 빠듯했던 게 맞았습니다. |
| [x] | 제출 슬라이드 3번 사실 오류 고치기 | Claude | 9/19 | 슬라이드가 `쇼핑몰 7곳에서 바로 찾아요`라고 적고, 품고 있던 스크린샷(`11-store-sheet.png`, 9/14판)에는 9/17에 뺀 홈플러스가 찍혀 있었습니다. 실제 `storeLinks.ts`는 쿠팡·네이버 쇼핑·컬리·이마트몰·롯데마트·G마켓 **6곳**입니다. 문구를 6곳으로 고치고, 운영 체험 계정에서 같은 시트를 1152×2496으로 다시 찍어 바꾼 뒤 슬라이드를 다시 구웠습니다(찍을 때 홈플러스가 없고 칩이 `낮은 가격순`인 것을 단언으로 확인). 심사위원이 운영에서 같은 화면을 열어도 이제 어긋나지 않습니다. |
| [x] | 1차 제출(지금 스크린샷·05 답안 그대로) | 사용자 | **9/16 오전** | 임시저장은 제출이 아니니 **제출** 버튼까지. 9/20까지 고칠 수 있습니다. 9/16 완료 |
| [x] | 서비스 링크 `https://galmuri-kitchen.onrender.com/?demo=1`을 폰 시크릿 탭에서 열어 로그인 화면 없이 재고 화면(체험 안내 카드)이 뜨는지 확인 | 사용자 | 최종 제출 직전, 9/20 23:59:59 전 한 번 더 | 9/18 사용자 폰 시크릿 탭 확인 완료(마감 직전 한 번 더 권장). 주최 측 안내(9/16): 로그인 화면이 먼저 뜨는 서비스는 로그인하지 않은 상태로도 쓸 수 있어야 합니다. 05 5절 링크가 이 주소이고, 이 기능(`feature/demo-link`)은 9/17 배포됐고, Claude가 새 브라우저로 로그인 화면 없이 재고 화면이 뜨는 것을 확인했습니다. 기본 주소는 지금처럼 로그인 화면입니다. |
| [ ] | 최종 제출(새 스크린샷·폼 이미지·답안으로 고쳐 다시 제출) | 사용자 | **9/20 마감 전** | 제출 직전 2절 환경변수 비교, 배포 주소가 열리는지 한 번 더 확인. 폼 이미지는 `form/`, 답안은 05. **폼에서 고칠 두 칸**: 서비스 링크를 `…onrender.com/?demo=1`로, AI 활용 칸을 [05의 500자 문안](05-제출-양식-답안.md)으로. 기술 스택 칩은 `Claude`·`React`만. 제출 뒤에는 고칠 수 없습니다. |

### 스크린샷: 낡은 것과 새로 필요한 것

9/16 밤, 운영 주소(https://galmuri-kitchen.onrender.com, main `eed1117` 배포본)의 체험 계정으로 Claude가 찍었습니다. Playwright 크롬, 384×832 화면 @3x·터치, 한국어·서울 시간(PC 컷만 1280×800 @2x·마우스)입니다.
실제 AI는 3번만 불렀습니다: 내 사진 2장 인식(`ereceipt.jpg`·`fridge.jpg`를 앨범으로), 예시 사진 읽기(모바일 영수증), AI 식단 초안. 체험 로그인은 2번(본 화면, 다크 식단) 썼습니다. 사진이 붙는 저장은 하지 않았고, 요리했어요는 저장 뒤 바로 되돌려 재고를 원래대로 돌렸습니다.
그대로 쓰는 파일: `00-splash`, `04-recommend`, `05-ai-recipes`, `05b-ai-recipes-full`, `06-recipe-detail`, `07-link-import`, `07a-link-import-sheet`, `09-seasoning-calc`, `10-shopping-list`, `10b-shopping-list-full`, `11-store-sheet`, `13-memo-page`, `14-memo-scan-review`, `15-offline-banner`, `18-fridge-dark`, `19-shopping-dark`, `20-recipe-detail-dark`, `form/02`.

**다시 찍은 것**

| 파일 | 지금 화면 | 결과 |
|---|---|---|
| `01-login.png` | 카카오·네이버·구글 로고, 미리보기 카드, `지난번에 이걸로 로그인했어요` | 교체 |
| `02-fridge.png` | 목록은 같고 날짜만 새로(체험 안내 카드는 닫은 뒤) | 교체 |
| `03-scan-review.png` | 내 사진 2장(영수증·냉장고)을 한 번에 읽은 결과. 구입일 칩 `1주 전`, 계란만 `어제 구입`, `기억 안 나요` | 교체 |
| `08-videos.png` | 여러 채널 영상이 섞이고 채널 칩이 짧은 이름 | 교체 |
| `12-stock-in.png` | 줄 설명이 `장보기에 적어 둔 곳`으로 바뀜, 수세미(생활용품) `산 것으로만 옮겨요`. 긴 캡처 대신 1152×2496 | 교체 |
| `16-more.png` | 아래쪽 화면: 하루 칼로리 목표, AI 사용량(오늘 쓴 횟수), 데이터 출처 줄, 이용약관·개인정보처리방침 링크 | 교체 |
| `17-export-sheet.png` | 식단·먹은 기록·요리 일기 줄 | 교체 |
| `form/00-대표이미지.png`, `form/00-대표이미지-1200x630.png` | 문구 `재고부터 레시피·장보기·식단·아낀 돈까지`, 폰 02·21(가운데)·32 | 다시 만듦 |
| `form/01-재고와-사진인식.png` | 폰 02·03·36, 예시 사진으로 해보기 | 다시 만듦 |
| `form/03-장보기와-오프라인.png`, `form/04-메모사진과-쇼핑몰.png` | `form/03-장보기.png` 한 장으로 합침(폰 15·14·11) | 지우고 새로 |
| `form/05-레시피-가져오기와-양념.png` | `form/05-요리일기와-집밥리포트.png`(폰 29·31·32)로 바꿈 | 지우고 새로 |

**새로 찍은 것**

| 파일 | 화면 | 쓰는 곳 |
|---|---|---|
| `21-meals-week.png` | 식단 주 보기: 하루 목표 1,716kcal, 날마다 합계 막대, AI 초안을 넣은 뒤 알림 줄 | README 식단, form/00·04 |
| `22-meals-month.png` | 식단 월 보기 | README 식단 |
| `23-meal-ai-draft.png` | AI 식단 초안: 칸마다 후보·1인분 kcal·체크·다른 걸로 | README 식단, form/04 |
| `24-meal-shopping.png` | 식단으로 장보기 목록 만들기: 모자란 만큼·살 날 | README 식단 |
| `26-recipe-add-sheet.png` | 레시피 추가 시트: 링크·글·사진·직접 쓰기 | README 레시피 |
| `27-meals-dark.png` | 다크 테마 식단 주 보기(새 체험 계정) | README 다크 표 |
| `28-cook-sheet.png` | 김치찌개 요리했어요 시트 | README 식단 |
| `29-cook-toast.png` | 저장 뒤 되돌리기 알림: 뺀 재료·아낀 돈 | README 식단, form/05 |
| `30-cook-diary.png` | 요리 일기 목록 | README 식단 |
| `31-cook-diary-detail.png` | 요리 일기 상세: 아낀 돈 내역 | README 식단, form/05 |
| `32-cook-report.png` | 집밥 리포트: 9월 약 25,200원 | README 식단, form/00·05 |
| `33-food-log-calendar.png` | 먹은 기록 달력: 하루 kcal, `요` 표시 | README 식단, form/04 |
| `34-demo-guide.png` | 재고 첫 방문 체험 안내 카드 | README 체험 방법 |
| `35-scan-sample.png` | 사진으로 추가 시트: 예시 사진 | README 재고 |
| `36-scan-sample-review.png` | 예시 영수증을 읽은 결과: 사진 띠·영수증 날짜·가격 | README 재고, form/01 |
| `37-nutrition.png` | 김치찌개 1인분 영양 카드(AI 없이 저장된 값으로 계산) | README 레시피 |
| `38-pc-home.png` | PC 화면: 왼쪽 소개 + 재고(2560×1600) | README 체험 방법 |

`25-scan-multi`는 `03-scan-review`가 여러 장 인식을 보여 줘서 따로 만들지 않았고, `26-recipe-photo-import`는 `26-recipe-add-sheet`로 찍었습니다.
`form/`은 대표 이미지와 5장입니다: 01 재고와 사진인식, 02 재고로 레시피(그대로), 03 장보기, 04 식단과 영양, 05 요리일기와 집밥리포트. 05 문서 6절 순서와 같습니다.

**9/17 다시 찍은 것** — 9/16 밤 이후 체험 식단이 바뀌어(끼니마다 저녁 전부·점심 2개 채움) 오늘 날짜 기준으로 다시 찍었습니다. 같은 조건(Playwright 크롬, 384×832 @3x·터치, PROD `/?demo=1`, 로그아웃 상태 새 컨텍스트, 실제 AI 호출 없음). `21-meals-week`·`22-meals-month`·`24-meal-shopping`은 같은 체험 계정 세션에서 하루 칼로리 목표를 정한 뒤(README 표 예시처럼 목표 카드가 채워지도록) 이어서 찍었고, `27-meals-dark`는 목표를 정하지 않은 새 세션(다크)입니다. `21`을 쓰는 `form/00-대표이미지.png`·`form/04-식단과-영양.png`도 `render.sh`로 다시 만들었습니다.
새 파일 `39-store-find-all.png`(장보기 → 쇼핑몰에서 한꺼번에 찾기 → 쿠팡 → 품목별 찾기 목록)을 README 장보기 표에 추가했습니다. 이 화면(③ 찾기 목록)은 쇼핑몰 이름 버튼이 보이지 않아 홈플러스가 빠져도 다시 찍을 필요가 없습니다.

## 7. 출시 전 확인(제출과 별개)

| 완료 | 할 일 | 담당 | 메모 |
|---|---|---|---|
| [x] | 양념 기본 비율 출처 확인 | 사용자 + Claude | **끝났습니다(9/19).** 16개 프리셋의 **양까지** 공개 데이터에 맞췄고, 화면 출처 줄에 어느 레시피에서 온 값인지 이름·기준까지 나옵니다. **7개**(제육볶음·불고기·초고추장·쌈장·잡채·나물무침·양념간장)는 **한식진흥원 한식 정밀레시피(공공누리 제1유형)** — 조리법에 `고추장 1큰술(16g)`처럼 기관이 정한 큰술 환산이 붙어 있어 우리가 환산할 필요가 없습니다. **8개**는 농정원 식품 레시피(data.mafra.go.kr, 사용자 인증키로 전량 수집, 레시피 537건·재료 6,104줄)입니다. 비빔국수만 어느 쪽에도 없어 그렇다고 적었고, 닭볶음탕은 대응 레시피가 없어 **닭갈비 양념으로 바꿨습니다**. 농정원 쪽은 `저작권표시+동일조건변경허락`이라 표를 옮기지 않고 수치 대조 출처로만 씁니다. 농진청 전통향토음식 DB는 제4유형(상업적 이용금지)이라 제외했습니다. 밥숟가락은 9/18에 **12ml로 확정**(통용 계량 안내: 가득 10~12ml, 깎으면 약 10ml, 1큰술 15ml ≈ 깎은 밥숟가락 1.5개). |
| [ ] | `갈무리부엌` 상표 조사(KIPRIS 9류·42류) | 사용자 | 9/18 1차 조사: 동일 상표 없음. 남은 확인은 **9류·42류에 `갈무리` 단독·결합 선행상표가 있는지** — `부엌`은 식별력이 약해 심사에서 요부가 `갈무리`로 잡힐 수 있습니다. 제휴 링크(광고·중개)가 있어 35류도 함께 검토합니다. **9/19: Claude가 KIPRIS를 직접 조회할 수 없습니다**(자바스크립트로 도는 화면이라 주소로 검색 결과를 받을 수 없음). 대신 5분 절차를 적어 둡니다. ① kipris.or.kr → `상표` 탭 → 상세검색. ② 상표명칭 칸에 `갈무리*`(앞부분 일치)로 먼저 넓게 보고, 다음에 `갈무리부엌`을 그대로. ③ 상품분류 칸에 **9류**(모바일 앱·소프트웨어), **42류**(SaaS·소프트웨어 개발·호스팅), **35류**(광고·제휴 중개 — 쿠팡 파트너스 링크가 있어 함께 봄)를 각각 넣어 세 번 돌립니다. ④ 볼 것은 **`부엌`을 뺀 `갈무리` 단독·결합 상표**입니다. `부엌`은 식별력이 약해 심사에서 요부가 `갈무리`로 잡힐 수 있습니다. ⑤ 결과마다 **상태**(등록·출원중·거절·소멸)와 **지정상품**을 봅니다. 소멸·거절은 걸림돌이 아니고, 등록·출원중이면서 지정상품이 겹치면 변리사 상담 대상입니다. ⑥ 세 류 모두 겹치는 등록·출원중 상표가 없으면 이번 제출에는 문제가 없습니다(제출은 상표 등록과 무관합니다). |

| [x] | 이용약관·개인정보처리방침 페이지 | Claude(초안) + 사용자 | `33ebc57`, 배포 주소 `/terms.html`·`/privacy.html` 200. 9/16 시행으로 식단·먹은 기록·요리 일기·내 몸 정보, AI로 보내는 항목, 국외 이전(제28조의8), 이메일 문의, 쿠팡 제휴 링크 표시를 반영(`docs/voting-readiness`). 문구 법률 검토는 사용자 몫 |
| [x] | 내 몸 정보 동의 받기 | Claude | **9/18 배포 완료**(`1ccca70`, 운영 반영 확인). 키·몸무게가 민감정보(개인정보 보호법 제23조)일 수 있어 별도 동의를 받습니다 — `하루 칼로리 목표 정하기` 시트에 동의 체크 한 개(체크 전에는 저장되지 않음), `privacy.html` 1번에 건강 관련 항목·목적·보관 기간. 이미 저장한 사람에게는 다시 묻지 않습니다(처음 저장할 때 동의). **동의 기록은 행의 존재 자체**입니다 — `body_profiles`에는 `created_at`이 없고 `updated_at`만 있어 고칠 때마다 갱신되므로 정확한 동의 시각이 필요해지면 `agreed_at` 컬럼을 따로 둡니다(투표 뒤). |
| [x] | 회원 탈퇴 | Claude | **9/18 배포 완료**(`1ccca70`, 운영에서 `DELETE /api/account` 로그인 전 401 확인). 더보기 `계정` → `회원 탈퇴` → 확인 시트(지워지는 것 목록 + 확인 체크) → `DELETE /api/account`. 체험 계정 정리와 같은 경로(users CASCADE + `photos.user_photo_keys` → `storage.delete`)를 씁니다. 소셜 연결 끊기는 하지 않습니다 — 다시 로그인하면 빈 새 계정이고, 방침 7번에 각 플랫폼에서 연결을 끊는 방법을 적어 두었습니다. 방침 7번의 이메일 삭제 요청도 남겨 뒀습니다. |
| [ ] | 운영 `body_profiles`에 9/18 이전 행이 있는지 확인 | 사용자 | 9/18 리뷰 지적. `SELECT id, user_id, updated_at FROM body_profiles;` — 동의 UI가 9/18에 들어와서, 그 전에 저장된 행의 주인은 동의를 받은 적이 없는데 다시 묻지 않습니다. 본인·테스트 계정뿐이면 그대로 두고, 남의 행이 있으면 그 행만 지우면 다음 접속 때 동의를 받습니다. |
| 보류 | 쿠팡 파트너스 안내 문구·실적 인정 범위 확인 | 사용자 | 키 발급 뒤. 코드는 준비됨(`9775aed`, `b1b7732`). 시트 아래 안내 문구가 파트너스 기준을 채우는지는 운영팀 판단입니다. |

## 8. 투표 대비 (9/17 밤 설정, 9/21~)

공개 투표가 시작되면 체험 계정·AI 호출이 한꺼번에 몰릴 수 있습니다. 담당은 따로 적지 않으면 사용자입니다. 2~5번은 9/17(목) 최종 제출 뒤에 합니다. 1번은 실제 폰이 필요해 9/18~20 중에 합니다.

| 완료 | 순서 | 할 일 | 담당 | 메모 |
|---|---|---|---|---|
| [x] | 1 | LTE 폰으로 체험하기 → 체험 계정 IP 한도 확인 | 사용자 | **9/19 밤 사용자가 와이파이를 끈 LTE 폰으로 `체험하기`를 열 번쯤 들락거렸고 모두 정상이었습니다** — `지금 같은 인터넷으로 체험하는 분이 많아요`가 뜨지 않았으니, 통신사 NAT 주소가 한도에 걸리지도, 모든 사람이 프록시 주소 하나로 묶이지도 않았습니다. 운영 한도가 시간당 100개(바로 아래 2번)라 열 번은 한참 아래입니다. [`docs/deploy.md`](../deploy.md) 5-3의 **앞 확인(꾸민 `X-Forwarded-For`로 한도+1번 보내 마지막이 429인지)은 돌리지 않았습니다** — 지금 한도면 101번을 보내야 하고, `TRUSTED_PROXY_HOPS`가 맞다면 그 101개가 사용자 집 주소로 세어져 제출 직전에 본인이 한 시간 막힙니다. 투표가 끝난 뒤에 합니다. |
| [x] | 2 | Render 환경 변수: `AI_DAILY_SCAN_LIMIT=5`, `AI_DAILY_RECIPE_LIMIT=5`, `DEMO_IP_HOURLY_LIMIT=100`, `DEMO_IP_DAILY_LIMIT=300` | 사용자 | **9/19 사용자가 넣었다고 알려 줬습니다**(대시보드는 사용자만 보므로 Claude가 확인한 값은 아닙니다). 앞의 둘은 로그인 사용자 1명당 하루 한도를 코드 기본 20→5로 낮춥니다. 전체 상한은 9/16에 넣은 `USER_AI_GLOBAL_DAILY=100`(`render.yaml`)이 따로 막지만, 1인당이 20/20이면 계정 셋이 하루 예산을 다 쓸 수 있어 함께 낮췄습니다. 뒤의 둘은 반대로 올리는 쪽입니다 — 코드 기본 IP 한도(1시간 30개·24시간 100개)로는 한 사무실 주소 뒤 심사위원 여럿이 막힐 수 있습니다. 네 변수 모두 `render.yaml`에 없어 대시보드에만 있습니다. |
| [ ] | 3 | Anthropic 콘솔 사용량 알림 켜기, 잔액 매일 확인 | 사용자 | 비상 스위치: `DEMO_AI_GLOBAL_DAILY=0`이면 체험 계정 전원이 예시 결과로 바뀝니다. `ANTHROPIC_API_KEY`는 지우지 않습니다(지우면 로그인 사용자까지 AI 버튼이 숨겨집니다). |
| [ ] | 4 | Render Shell에서 `ai_calls`의 kind별 호출 수·평균 토큰 실측 → `DEMO_AI_GLOBAL_DAILY` 다시 정하기 | 사용자 | 실패한 호출은 모델·토큰이 비어 있으니 평균에서 뺍니다. `link_miss`처럼 `_miss`로 끝나는 kind(찾지 못함·오류)는 `_miss`를 떼어 원래 kind와 합쳐 셉니다. |
| [ ] | 5 | 마지막 배포 뒤 Render **Auto-Deploy Off** | 사용자 | **9/20(일)에 끄기로 함**(9/18 결정, 9/19 밤 사용자가 다시 확인) — 9/18 현재 켜져 있어 push하면 약 80초 뒤 자동 배포됩니다. 심사 중 push가 배포되면서 진행 중인 AI 요청이 끊기는 것을 막습니다. 옆 세션(다른 Claude 작업)에도 알립니다. **사용자가 마지막에 직접 처리합니다(9/18 결정) — Claude는 이 항목을 따로 묻지 않습니다.** |
| [ ] | 6 | Render 로그에서 `failed:` 매일 검색 | 사용자 | AI 호출 실패(`scan receipt failed`, `meal draft failed`, `nutrition estimate failed` 등), 링크·식품 DB 요청 실패(`import fetch failed`, `food fetch failed`), 소셜 로그인 실패(`oauth … callback failed`)가 이 글자로 남습니다. |
| [x] | 7 | `product.html` 바닥글 `[회사명]`·`[제휴·투자 문의]` 채우기(9/16: `개인 프로젝트`, 같은 문의 메일) | 사용자 | `[문의 이메일]`은 ggamjige8888@naver.com으로 채웠습니다. 나머지 두 칸은 값을 받아야 합니다. |
